name: JIT memory service check

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: gated-private-jit-memory-check
  cancel-in-progress: false

jobs:
  check:
    name: Check private JIT memory source
    runs-on: macos-26
    timeout-minutes: 30
    environment: private-build

    steps:
      - name: Create short-lived repository token
        id: app-token
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        with:
          app-id: ${{ secrets.BUILD_APP_ID }}
          private-key: ${{ secrets.BUILD_APP_PRIVATE_KEY }}
          owner: ${{ secrets.PRIVATE_OWNER }}
          repositories: ${{ secrets.PRIVATE_REPOSITORY }}

      - name: Check out approved private source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: ${{ secrets.PRIVATE_OWNER }}/${{ secrets.PRIVATE_REPOSITORY }}
          ref: ${{ secrets.PRIVATE_REF }}
          token: ${{ steps.app-token.outputs.token }}
          persist-credentials: false
          submodules: false
          fetch-depth: 1
          path: private-source

      - name: Compile and test the service
        shell: bash
        working-directory: private-source
        run: bash ci/hosted-jit-memory-check.sh
